{% extends 'JarryUbuBundle::masterlayout.html.twig' %}

{% block title %}
    UBU see a Zone
{% endblock %}

{% block titre %}
    UBU Zone
{% endblock %}

{% block status %}
    Nom DU TYPE
{% endblock %}

{% block navigationZone %}
    <a href="{{ path('jarry_ubu_homepage') }}"><li class="{{ navDarkCss }}">LOGOUT</li></a>
    <a href="{{ path('ubu_place') }}"><li class="{{ navCss }}">Liste des places</li></a>
<<<<<<< HEAD
    <a href="{{ path('ubu_place') }}{{ entity.place.id }}"><li class="{{ navCss }}">Retour à la Place {{ entity.place.name }}</li></a>
     <a href="{{ path('zone_edit', { 'id' : entity.id }) }}"><li class="{{ navCss }}">Editer la zone {{ entity.name }}</li></a>
=======
>>>>>>> nodes étape 1
    <a href="{{ path('jarry_ubu_homepage') }}"><li class="{{ navDarkCss }}">Mentions légales</li></a>
{% endblock %}

{% block content -%}
<main class="mdl-layout__content fond">
    <div class="mdl-grid demo-content">
        <div class="{{ titreCss }}">
            <h3 class="centrer">{{ entity.name|upper| replace({ ("_") : " "}) }}</h3>
        </div>
            {# {% include "JarryUbuBundle:Zone:index.html.twig" %} #}
            <div class="{{ containerCss }}">
                
                        <a href="{{ path('ubu_place_show', { 'id' : entity.place.id }) }}" class="{{ btnCss }}">
                            PLACE {{ entity.place.name }}
                        </a>
              
                        <a href="{{ path('zone_edit', { 'id': entity.id }) }}"  class="{{ btnCss }}">
                            ÉDITER
                        </a>
                
                    
                   {{ form(delete_form) }}
            </div>
    </div>
</main>
{% endblock %}
