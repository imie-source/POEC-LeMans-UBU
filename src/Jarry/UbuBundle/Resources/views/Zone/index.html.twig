{% block zone %}
    
                <div class="{{ titreCss }}">
                    <img src="{{ asset('bundles/jarryubu/images/zone.png') }}"/>
                    <h3 class="centrer">LISTE DES ZONES</h3>
                </div>
                <div class="{{ containerCss }}"> 
                    {% for zone in entity.zones %}
                        <a href="{{ path('zone_show', { 'id': entity.id }) }}">
                            <button class="{{ carreClicCss }}">
                                <img src="{{ asset('bundles/jarryubu/images/zone.png') }}"/>
                            
                                <h4>{{ zone.name | replace({ ("_") : " "}) }}</h4>
                                <p>{{ zone.getInOutStatus ? 'Extérieur' : 'Intérieur' }}</p>
                            </button>
                        </a>
                    {% endfor %}
                    <a href="{{ path('zone_new') }}">
                        <button class="{{ carreNewCss }}">
                            <h4>Créer une nouvelle zone</h4>
                        </button>
                    </a>
                </div>
      
    {% endblock %}
