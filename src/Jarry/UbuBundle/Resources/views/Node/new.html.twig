{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Node creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('zone_show', { 'id': idZone, 'idPlace': idPlace }) }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
