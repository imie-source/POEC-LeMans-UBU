{% block node %}
    
                <div>
                    <h5>LISTE DES NODES</h5>
                </div>
                <div class="{{ containerCss }}"> 
                    {% for node in entity.nodes %}
                        <a href="{{ path('node_show', { 'id': node.id }) }}">
                            <button class="{{ carreClicCss }}">
                                <h4>{{ node.name | replace({ ("_") : " "}) }}</h4>
                            </button>
                        </a>
                    {% endfor %}
                    <a href="{{ path('node_new') }}">
                        <button class="{{ carreNewCss }}">
                            <h4>Créer une nouvelle node</h4>
                        </button>
                    </a>
                </div>
      
    {% endblock %}
